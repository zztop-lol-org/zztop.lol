{
  "name": "zztop-tools",
  "private": true,
  "type": "module",
  "description": "Local build/test tooling (gitignored deps). Not deployed.",
  "scripts": {
    "test": "node test/derive.test.mjs && node test/engine.test.mjs",
    "build:worker": "esbuild src/worker.src.js --bundle --format=iife --outfile=vanity-worker.js --target=es2020 --minify"
  },
  "dependencies": {
    "@noble/curves": "1.9.4",
    "@noble/hashes": "1.8.0",
    "bech32": "2.0.0",
    "esbuild": "0.24.0",
    "ethers": "6.13.4"
  }
}
